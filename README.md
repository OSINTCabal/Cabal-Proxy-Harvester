# Cabal-Proxy-Harvester
Python tool that crawls public proxies, tests them, and then creates a live database of proxies found to be functional/usable. Great for if you just need some quick proxies that actually work.
